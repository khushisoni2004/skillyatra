const express=require('express'); const Progress=require('../models/Progress'); const PracticeAttempt=require('../models/PracticeAttempt'); const {protect}=require('../middleware/authMiddleware'); const router=express.Router();
router.get('/',protect,async(req,res)=>{const solved=await Progress.countDocuments({userId:req.user._id,itemType:'DSA Question',solved:true}); const attempts=await PracticeAttempt.find({userId:req.user._id}); const acc=attempts.length?attempts.filter(a=>a.isCorrect).length/attempts.length:0; const score=Math.min(100,Math.round(solved*2 + acc*40 + (req.user.profile?.college?10:0))); res.json({score,weakAreas:req.user.profile?.weakAreas||['DP','Aptitude'],skillDNA:{dsa:solved,mcqAccuracy:Math.round(acc*100),target:req.user.profile?.targetCompany||'TCS'}});});
module.exports=router;
